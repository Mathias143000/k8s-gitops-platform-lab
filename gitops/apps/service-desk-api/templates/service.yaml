apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-desk-api.fullname" . }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "service-desk-api.fullname" . }}
  ports:
    - name: http
      port: 80
      targetPort: http


apiVersion: v1
kind: Service
metadata:
  name: {{ include "webhook-ingestion-service.fullname" . }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "webhook-ingestion-service.fullname" . }}
  ports:
    - name: http
      port: 80
      targetPort: http

